<!DOCTYPE html>
<html>
  <head>
    <%= csrf_meta_tags %>

    <meta http-equiv="Content-Type" content="text/html; charset=Shift_JIS" />

    <%= display_meta_tags site: 'Swift',
                      reverse: true,
                      title: '人材派遣・業務請負はスイフトへ',
                      description: '「今」の価値を最大化するため、単なる仕事の橋渡しだけでなく、人材を扱うプロフェッショナルとしてゼロから経験と技術を兼ね備えたエキスパートを教育し提供できる価値を高めることにも取り組んでいます。',
                      keywords: '人材派遣　業務請負'
                      %>
  </head>

  <body>  

    <h3>ログインする</h3>

    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

        <%= f.input :email, required: false, autofocus: true, placeholder: 'メールアドレス', label: false %>
        <%= f.input :password, required: false, placeholder: 'パスワード', label: false %>
        <%= f.input :remember_me, as: :boolean, label: 'ブラウザを閉じるときオートログインを有効にする' if devise_mapping.rememberable? %>

        <%= f.button :submit, "ログイン" %>

    <% end %>
    <br>
    <%= render "devise/shared/links" %>

  </body>
</html>
